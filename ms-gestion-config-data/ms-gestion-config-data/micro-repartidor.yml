server:
  port: 8082
  servlet:
    context-path: /micro-repartidor
    
  error:
    include-details: always
       
# Registry Client
eureka:
   client:
      fetch-registry: true
      serviceUrl:
         defaultZone: http://localhost:8889/eureka

    